let skills:string[] = [];
skills.push('A');
skills.push('B')

console.log(skills);
console.log(typeof(skills))
console.log(typeof(skills[0]))

let series = [1,2,3];
let double_series = series.map((item)=>item * 2);
console.log(series, double_series)

